/**
 * FILE NÀY ĐÃ BỊ LOẠI BỎ (DEPRECATED).
 *
 * Cấu trúc mới (theo yêu cầu tái cấu trúc) đã di chuyển logic API 
 * cho "collaboration" vào thư mục `api/` của feature:
 *
 * -> src/features/collaboration/api/collaborationApi.ts
 * (File này đã được tạo ở Giai đoạn 3b)
 *
 * File này được giữ lại để khớp với danh sách file trống
 * và sẽ không được sử dụng trong ứng dụng.
 */

// Deprecated
