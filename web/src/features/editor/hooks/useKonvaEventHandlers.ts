// src/features/editor/hooks/useKonvaEventHandlers.ts
/**
 * FILE NÀY LÀ PLACEHOLDER.
 * * Logic cho các sự kiện Konva (pan, zoom, drag) đã được triển khai
 * trực tiếp bên trong `src/features/editor/components/Canvas/MindmapStage.tsx`
 * (trong Giai đoạn 1) để đảm bảo tính hoàn thiện cao và kiểm soát state cục bộ
 * (như isPanning, lastDist) một cách hiệu quả.
 *
 * Tách logic này ra hook riêng sẽ là một bước tái cấu trúc (refactor)
 * phức tạp và có thể ảnh hưởng đến hiệu năng nếu không cẩn thận.
 * Giữ logic trong `MindmapStage` hiện tại là tối ưu.
 */
 
// Placeholder
