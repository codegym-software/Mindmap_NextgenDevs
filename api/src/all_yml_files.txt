==== ./main/resources/application-prod.yml ====
# # ===================================================================
# # PRODUCTION-READY CONFIGURATION (Dùng Environment Variables)
# # ===================================================================

# server:
#   port: ${SERVER_PORT:8081}

# spring:
#   data:
#     mongodb:
#       uri: ${MONGO_URI} # Bắt buộc phải có

#   security:
#     oauth2:
#       resourceserver:
#         jwt:
#           issuer-uri: ${APP_SECURITY_ISSUER} # Bắt buộc
#           jwk-set-uri: ${APP_SECURITY_JWK_SET_URI} # Bắt buộc

# app:
#   # --- Cấu hình AWS SDK (Mới Giai đoạn 2) ---
#   aws:
#     region: ${APP_AWS_REGION} # <--- THÊM MỚI (Bắt buộc)

#   security:
#     frontend-client-id: ${APP_SECURITY_FRONTEND_CLIENT_ID} # Bắt buộc
    
#     # --- Cấu hình Guest JWT (Mới Giai đoạn 2) ---
#     guest-jwt-secret: ${APP_SECURITY_GUEST_JWT_SECRET} # <--- THÊM MỚI (Bắt buộc, rất quan trọng)
#     guest-jwt-issuer: ${APP_SECURITY_GUEST_JWT_ISSUER:https://api.yourdomain.com/guest} # <--- THÊM MỚI
#     guest-jwt-expiration-ms: ${APP_SECURITY_GUEST_JWT_EXP_MS:86400000} # <--- THÊM MỚI
    
#   cors:
#     allowed-origins: ${APP_CORS_ALLOWED_ORIGINS} # Bắt buộc

#   frontend-base-url: ${APP_FRONTEND_BASE_URL} # <--- THÊM MỚI (Bắt buộc)

#   rate-limit:
#     enabled: true
#     requests-per-minute: 200

# logging:
#   level:
#     root: INFO
#     com.example.mindmap: INFO
#     org.springframework.security: WARN

==== ./main/resources/application-dev.yml ====
server:
  port: 8081

spring:
  data:
    mongodb:
      uri: mongodb://mongo:27017/mindmapdb
  security:
    oauth2:
      resourceserver:
        jwt:
          # [FIX] Di chuyển jwk-set-uri từ 'app.security' lên đây
          issuer-uri: ${app.security.issuer}
          jwk-set-uri: https://cognito-idp.ap-southeast-2.amazonaws.com/ap-southeast-2_t30OWWizg/.well-known/jwks.json

app:
  security:
    issuer: https://cognito-idp.ap-southeast-2.amazonaws.com/ap-southeast-2_t30OWWizg
    frontend-client-id: 7so8qnoi7pgne6bq0kq2ppharm
    # jwk-set-uri: ... (Đã bị xóa khỏi đây)
  cors:
    allowed-origins: http://localhost:3000,http://127.0.0.1:3000

  # [FIX LỖI 429] Thêm vào để tắt Rate Limit khi chạy profile 'dev'
  rate-limit:
    enabled: false

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.web: DEBUG
    org.springframework.security.oauth2: DEBUG
    org.springframework.security.oauth2.server.resource: DEBUG
    org.springframework.web: DEBUG
    com.nimbusds.jose: DEBUG
    com.example.mindmap: DEBUG
==== ./main/resources/application.yml ====
server:
  port: 8081

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/mindmapdb
  security:
    oauth2:
      resourceserver:
        jwt:
          # [FIX] Di chuyển jwk-set-uri từ 'app.security' lên đây
          issuer-uri: ${app.security.issuer}
          jwk-set-uri: https://cognito-idp.ap-southeast-2.amazonaws.com/ap-southeast-2_t30OWWizg/.well-known/jwks.json

app:
  security:
    issuer: https://cognito-idp.ap-southeast-2.amazonaws.com/ap-southeast-2_t30OWWizg
    frontend-client-id: 7so8qnoi7pgne6bq0kq2ppharm
    # jwk-set-uri: ... (Đã bị xóa khỏi đây)
  cors:
    allowed-origins: http://localhost:3000,http://127.0.0.1:3000

  # [FIX LỖI 429] Thêm vào để tắt Rate Limit khi chạy profile 'default'
  rate-limit:
    enabled: false

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.web: DEBUG
    org.springframework.security.oauth2: DEBUG
    org.springframework.security.oauth2.server.resource: DEBUG
    org.springframework.web: DEBUG
    com.nimbusds.jose: DEBUG
    com.example.mindmap: DEBUG
