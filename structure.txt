web/
├─ src/
│  ├─ app/
│  │  ├─ routes.tsx                # Khai báo route (Dashboard, Editor…)
│  │  ├─ providers/
│  │  │  ├─ AuthProvider.tsx        # Context: auth + tokens + refresh
│  │  │  └─ ThemeProvider.tsx       # Dark/Light
│  │  └─ store/
│  │     ├─ useMindmapsStore.ts     # Zustand store: danh sách mindmap, loading, error
│  │     └─ useEditorStore.ts       # Zustand store: state editor (nodes, edges, undo/redo)
│  ├─ auth/
│  │  ├─ cognito.ts                 # SDK, login/logout, exchange, refresh
│  │  └─ guards/ProtectedRoute.tsx  # Bảo vệ route (chỉ cần access token)
│  ├─ components/
│  │  ├─ layout/
│  │  │  ├─ Header.tsx              # Logo trái, Login/Signup hoặc Avatar/Logout phải
│  │  │  └─ Sidebar.tsx             # 3 chấm (pin/expand), 3 phần: actions / recent / account
│  │  ├─ dashboard/
│  │  │  ├─ BigStartButton.tsx      # “Bắt đầu tạo Mindmap ngay”
│  │  │  └─ MindmapCard.tsx         # Card 7 mindmap (auto marquee text)
│  │  ├─ editor/
│  │  │  ├─ EditorToolbar.tsx       # undo/redo/share/theme/save
│  │  │  ├─ DisplaySwitcher.tsx     # mũi tên lên (chọn kiểu layout tree/radial…)
│  │  │  ├─ Canvas.tsx              # Render mindmap (Konva/ReactFlow/custom)
│  │  │  └─ SidebarPinButton.tsx
│  │  └─ common/
│  │     ├─ Icon.tsx
│  │     ├─ Button.tsx
│  │     └─ Spinner.tsx
│  ├─ hooks/
│  │  ├─ useAuth.ts                 # Trả access token + refresh nếu sắp hết hạn
│  │  ├─ useSync.ts                 # Đồng bộ local ↔ server sau login
│  │  └─ useLocalMindmap.ts         # CRUD mindmap guest (localStorage)
│  ├─ pages/
│  │  ├─ Dashboard.tsx              # Mặc định khi vào / (chưa login cũng vào đây)
│  │  ├─ Editor.tsx                 # Ẩn header, show toolbar + canvas
│  │  ├─ Callback.tsx               # Xử lý code → token, set mm_tokens
│  │  └─ Logout.tsx
│  ├─ services/
│  │  ├─ api.ts                     # Axios (baseURL /api), interceptor Authorization
│  │  ├─ mindmapsApi.ts             # Gọi BE: list/create/update/delete
│  │  └─ authStorage.ts             # get/set mm_tokens (access/id/refresh/exp)
│  ├─ styles/
│  │  └─ globals.css                # Tailwind @layer + utility
│  ├─ main.tsx
│  └─ App.tsx
├─ index.html
├─ tailwind.config.js
├─ postcss.config.js
└─ vite.config.ts


api/
├─ src/main/java/com/example/mindmap/
│  ├─ MindmapApiApplication.java
│  ├─ config/
│  │  ├─ SecurityConfig.java       # Resource Server (Bearer)
│  │  ├─ JwtConfig.java            # JwtDecoder + issuer + audience validator
│  │  └─ RateLimitFilter.java
│  ├─ controller/
│  │  ├─ MindmapController.java    # /api/mindmaps
│  │  └─ UserController.java       # /api/me
│  ├─ service/
│  │  ├─ MindmapService.java
│  │  └─ UserService.java
│  ├─ repository/
│  │  └─ MindmapRepository.java
│  ├─ model/
│  │  └─ Mindmap.java              # id, userId?, nodes[], edges[], meta, updatedAt
│  └─ dto/
│     ├─ MindmapUpsertRequest.java
│     └─ MindmapResponse.java
├─ src/main/resources/
│  ├─ application.yml
│  └─ application-dev.yml
└─ pom.xml
